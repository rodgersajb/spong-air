import { Html, Body, Head, Heading, Hr, Container, Preview, Section, Text } from "@react-email/components"

export default function EmailContact({message}) {
    return <Html>
        <Head />
        <performance
    </Html>
}